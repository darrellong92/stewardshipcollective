<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Stewardship Collective — Singapore</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;1,400;1,500&family=Inter:wght@300;400;500;600&family=DM+Sans:ital,wght@0,300;0,400;1,300;1,400&display=swap" rel="stylesheet">
<style>
  :root {
    --sc-deep:    #1A2E3B;
    --sc-dusk:    #2A4558;
    --sc-current: #3D6478;
    --sc-sky:     #5C8FA6;
    --sc-horizon: #85B4C8;
    --sc-mist:    #B8D4E0;
    --sc-cloud:   #DFF0F5;
    --sc-breath:  #EEF7FA;
    --sc-snow:    #F6FAFB;
    --sc-clay:    #A08060;
    --sc-wheat:   #C9A96E;
    --sc-sand:    #E8D9C0;
    --sc-pine:    #2D5C3E;
    --sc-fern:    #4A7C5E;
    --sc-moss:    #7DAF90;
    --sc-meadow:  #C8E4D2;
    --sc-ink:     #1E2C35;
    --sc-body:    #3A4B55;
    --sc-muted:   #6B7D87;
    --sc-quiet:   #94A8B2;
    --sc-border:  #D8E8EE;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    font-family: 'Inter', sans-serif;
    background: var(--sc-snow);
    color: var(--sc-body);
    line-height: 1.75;
    -webkit-font-smoothing: antialiased;
  }

  /* ═══════════════════════════════════════════════════
     STICKY NAV
  ═══════════════════════════════════════════════════ */
  .site-nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 1000;
    background: rgba(26,46,59,0.96);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(133,180,200,0.12);
    height: 56px;
    display: flex;
    align-items: center;
    padding: 0 48px;
    gap: 0;
  }

  .nav-wordmark {
    display: flex;
    flex-direction: column;
    gap: 1px;
    margin-right: auto;
    text-decoration: none;
  }

  .nav-wordmark-name {
    font-family: 'Lora', serif;
    font-size: 14px;
    font-weight: 400;
    color: #F6FAFB;
    letter-spacing: 0.01em;
    line-height: 1;
  }

  .nav-wordmark-sub {
    font-size: 7.5px;
    font-weight: 600;
    letter-spacing: 3.5px;
    text-transform: uppercase;
    color: var(--sc-horizon);
    line-height: 1;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .nav-link {
    font-family: 'Inter', sans-serif;
    font-size: 12px;
    font-weight: 500;
    color: var(--sc-horizon);
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 4px;
    letter-spacing: 0.02em;
    transition: all 0.15s ease;
    white-space: nowrap;
  }

  .nav-link:hover {
    color: #F6FAFB;
    background: rgba(133,180,200,0.1);
  }

  .nav-link.active {
    color: #F6FAFB;
    background: rgba(92,143,166,0.18);
  }

  .nav-sep {
    width: 1px;
    height: 16px;
    background: rgba(133,180,200,0.2);
    margin: 0 4px;
  }

  /* Offset all scroll targets so nav doesn't obscure them */
  [id] { scroll-margin-top: 72px; }

  /* ═══════════════════════════════════════════════════
     SHARED SECTION STYLES
  ═══════════════════════════════════════════════════ */
  .section {
    padding: 96px 72px;
    border-top: 1px solid var(--sc-border);
  }

  .section-header {
    padding: 72px 72px 48px;
    border-top: 3px solid var(--sc-current);
    background: var(--sc-deep);
  }

  .section-header-inner {
    max-width: 1060px;
    margin: 0 auto;
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 32px;
  }

  .section-header h2 {
    font-family: 'Lora', serif;
    font-size: clamp(28px, 3.5vw, 44px);
    font-weight: 400;
    color: #F6FAFB;
    letter-spacing: -0.01em;
    margin-bottom: 0;
  }

  .section-header h2 em {
    font-style: italic;
    color: var(--sc-mist);
  }

  .section-header p {
    font-size: 15px;
    color: var(--sc-horizon);
    line-height: 1.85;
    max-width: 480px;
    flex-shrink: 0;
  }

  .inner       { max-width: 1060px; margin: 0 auto; }
  .inner-narrow{ max-width: 720px;  margin: 0 auto; }

  .tinted   { background: var(--sc-breath); }
  .sky-tint { background: var(--sc-cloud); }
  .dark     { background: var(--sc-deep); border-top: none; }

  .label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 3.5px;
    text-transform: uppercase;
    color: var(--sc-sky);
    margin-bottom: 18px;
    display: block;
  }

  .label-light { color: var(--sc-horizon); }

  h2 {
    font-family: 'Lora', serif;
    font-size: clamp(27px, 3.2vw, 40px);
    font-weight: 400;
    line-height: 1.25;
    color: var(--sc-ink);
    margin-bottom: 28px;
    letter-spacing: -0.01em;
  }

  h2 em { font-style: italic; color: var(--sc-current); }
  h2.light { color: #F6FAFB; }
  h2.light em { color: var(--sc-mist); }

  .body-text {
    font-size: 15.5px;
    color: var(--sc-muted);
    line-height: 1.9;
    margin-bottom: 20px;
    max-width: 680px;
  }

  .body-text.light { color: var(--sc-horizon); max-width: 640px; }

  /* ═══════════════════════════════════════════════════
     HERO
  ═══════════════════════════════════════════════════ */
  .hero {
    min-height: 100vh;
    padding-top: 56px; /* nav height */
    background: linear-gradient(168deg,
      var(--sc-deep) 0%, var(--sc-dusk) 38%,
      var(--sc-current) 68%, var(--sc-sky) 100%);
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: end;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 50%;
    background:
      linear-gradient(158deg, transparent 18%, rgba(26,46,59,0.38) 40%, transparent 62%),
      linear-gradient(202deg, transparent 28%, rgba(42,69,88,0.2) 52%, transparent 76%);
    pointer-events: none;
  }

  .hero-orb {
    position: absolute; top: 8%; right: 6%;
    width: 420px; height: 420px; border-radius: 50%;
    background: radial-gradient(circle at 38% 38%, rgba(133,180,200,0.2) 0%, rgba(92,143,166,0.08) 48%, transparent 72%);
    pointer-events: none;
  }

  .hero-orb2 {
    position: absolute; bottom: -100px; left: 5%;
    width: 600px; height: 300px; border-radius: 50%;
    background: radial-gradient(ellipse at 50% 30%, rgba(92,143,166,0.1) 0%, transparent 65%);
    pointer-events: none;
  }

  .hero-left {
    position: relative; z-index: 2;
    padding: 80px 64px 88px;
    display: flex; flex-direction: column; justify-content: flex-end;
    min-height: calc(100vh - 56px);
  }

  .hero-right {
    position: relative; z-index: 2;
    padding: 80px 64px 88px 48px;
    min-height: calc(100vh - 56px);
    display: flex; flex-direction: column; justify-content: flex-end;
    border-left: 1px solid rgba(133,180,200,0.12);
  }

  .overline {
    display: inline-block;
    font-size: 10px; font-weight: 600; letter-spacing: 3.5px;
    text-transform: uppercase; color: var(--sc-mist);
    background: rgba(133,180,200,0.12); border: 1px solid rgba(133,180,200,0.25);
    padding: 6px 16px; border-radius: 3px; margin-bottom: 36px; width: fit-content;
  }

  .hero h1 {
    font-family: 'Lora', serif;
    font-size: clamp(36px, 5vw, 60px); font-weight: 400;
    line-height: 1.14; color: #F6FAFB; letter-spacing: -0.015em; margin-bottom: 0;
  }

  .hero h1 em { font-style: italic; color: var(--sc-mist); }

  .hero-right-label {
    font-size: 10px; font-weight: 600; letter-spacing: 3px;
    text-transform: uppercase; color: var(--sc-horizon);
    margin-bottom: 22px; display: block; opacity: 0.7;
  }

  .hero-descriptor {
    font-family: 'Lora', serif; font-size: 22px; font-weight: 400;
    font-style: italic; color: var(--sc-mist); line-height: 1.55; margin-bottom: 36px;
  }

  .hero-body {
    font-size: 16px; color: var(--sc-mist); line-height: 1.95; max-width: 440px; font-weight: 400;
  }

  /* ═══════════════════════════════════════════════════
     DEVOTIONAL
  ═══════════════════════════════════════════════════ */
  .devotional {
    padding: 96px 72px; background: #F0F5F7;
    border-top: none; position: relative;
  }

  .devotional::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
    background: linear-gradient(to right, transparent 0%, var(--sc-border) 20%, var(--sc-border) 80%, transparent 100%);
  }

  .devotional-inner { max-width: 680px; margin: 0 auto; }

  .devotional-label {
    font-size: 10px; font-weight: 600; letter-spacing: 3.5px;
    text-transform: uppercase; color: var(--sc-quiet); margin-bottom: 44px; display: block;
  }

  .scripture-card {
    margin-bottom: 52px; padding: 44px 52px; background: white;
    border: 1px solid var(--sc-border); border-radius: 16px;
    position: relative; overflow: hidden;
  }

  .scripture-card::before {
    content: ''; position: absolute; top: -40px; right: -40px;
    width: 160px; height: 160px; border-radius: 50%;
    background: radial-gradient(circle, rgba(92,143,166,0.07) 0%, transparent 70%);
    pointer-events: none;
  }

  .scripture-ref {
    font-size: 10px; font-weight: 600; letter-spacing: 3px;
    text-transform: uppercase; color: var(--sc-sky); margin-bottom: 28px; display: block;
  }

  .scripture-verses { list-style: none; display: flex; flex-direction: column; }

  .scripture-verse {
    display: grid; grid-template-columns: 28px 1fr; gap: 4px;
    padding: 9px 0; border-bottom: 1px solid #EEF5F8;
  }

  .scripture-verse:last-child { border-bottom: none; padding-bottom: 0; }

  .verse-num {
    font-size: 11px; font-weight: 600; color: var(--sc-horizon);
    padding-top: 3px; line-height: 1.7; opacity: 0.8;
  }

  .verse-text {
    font-family: 'Lora', serif; font-size: 15.5px; font-weight: 400;
    color: var(--sc-ink); line-height: 1.78;
  }

  .verse-text em { font-style: italic; color: var(--sc-current); }

  .devotional-body {
    font-family: 'Lora', serif; font-size: 16px; font-weight: 400;
    color: var(--sc-body); line-height: 1.95; margin-bottom: 22px;
  }

  .devotional-closing {
    margin-top: 36px; padding-top: 32px; border-top: 1px solid var(--sc-border);
    font-family: 'DM Sans', sans-serif; font-size: 13.5px; font-style: italic;
    color: var(--sc-quiet); line-height: 1.8;
  }

  /* ═══════════════════════════════════════════════════
     OVERVIEW: TENSION SPLIT
  ═══════════════════════════════════════════════════ */
  .tension-split {
    display: grid; grid-template-columns: 1fr 64px 1fr;
    gap: 0; margin-top: 52px; align-items: stretch;
  }

  .tension-side {
    background: white; border: 1px solid var(--sc-border);
    border-radius: 14px; padding: 40px 40px 44px;
  }

  .tension-divider {
    display: flex; flex-direction: column;
    align-items: center; justify-content: center; position: relative;
  }

  .tension-divider::before {
    content: ''; position: absolute; top: 0; bottom: 0; left: 50%; width: 1px;
    background: linear-gradient(to bottom, transparent 0%, var(--sc-border) 20%, var(--sc-border) 80%, transparent 100%);
  }

  .gap-pill {
    position: relative; z-index: 1; background: var(--sc-snow);
    border: 1px solid var(--sc-border); border-radius: 40px;
    padding: 10px 0; width: 52px; display: flex; flex-direction: column;
    align-items: center;
  }

  .gap-pill-text {
    font-size: 8px; font-weight: 600; letter-spacing: 1.5px;
    text-transform: uppercase; color: var(--sc-quiet);
    writing-mode: vertical-rl; text-orientation: mixed;
    transform: rotate(180deg); line-height: 1;
  }

  .tension-icon {
    width: 40px; height: 40px; border-radius: 10px;
    margin-bottom: 18px; display: flex; align-items: center; justify-content: center;
  }

  .tension-icon.sky   { background: rgba(92,143,166,0.1); }
  .tension-icon.green { background: rgba(74,124,94,0.1); }
  .tension-icon svg   { width: 20px; height: 20px; }

  .tension-side-label {
    font-size: 10px; font-weight: 600; letter-spacing: 3px;
    text-transform: uppercase; margin-bottom: 14px; display: block;
  }

  .tension-side-label.sky   { color: var(--sc-sky); }
  .tension-side-label.green { color: var(--sc-fern); }

  .tension-side h3 {
    font-family: 'Lora', serif; font-size: 21px; font-weight: 400;
    color: var(--sc-ink); line-height: 1.35; margin-bottom: 18px;
  }

  .tension-list { list-style: none; display: flex; flex-direction: column; gap: 12px; }

  .tension-list li {
    font-size: 14px; color: var(--sc-muted); line-height: 1.75;
    padding-left: 20px; position: relative;
  }

  .tension-list li::before {
    content: '—'; position: absolute; left: 0;
    color: var(--sc-border); font-weight: 300;
  }

  /* ═══════════════════════════════════════════════════
     OVERVIEW: HOW CARDS
  ═══════════════════════════════════════════════════ */
  .how-cards { display: flex; flex-direction: column; gap: 20px; margin-top: 52px; }

  .how-card {
    display: grid; grid-template-columns: 280px 1fr;
    border: 1px solid var(--sc-border); border-radius: 14px;
    overflow: hidden; background: white;
  }

  .how-card-left {
    padding: 40px 36px; border-right: 1px solid var(--sc-border);
    display: flex; flex-direction: column; justify-content: space-between;
  }

  .how-card-left.sky   { background: rgba(92,143,166,0.04); }
  .how-card-left.green { background: rgba(74,124,94,0.04); }
  .how-card-left.wheat { background: rgba(201,169,110,0.04); }

  .how-card-num {
    font-family: 'Lora', serif; font-size: 56px; font-weight: 400;
    line-height: 1; opacity: 0.14; margin-bottom: 24px;
  }

  .how-card-left.sky   .how-card-num { color: var(--sc-current); }
  .how-card-left.green .how-card-num { color: var(--sc-pine); }
  .how-card-left.wheat .how-card-num { color: var(--sc-clay); }

  .how-card-title {
    font-family: 'Lora', serif; font-size: 20px; font-weight: 500;
    color: var(--sc-ink); line-height: 1.35; margin-bottom: 16px;
  }

  .how-card-tag {
    display: inline-block; font-size: 10px; font-weight: 600;
    letter-spacing: 2.5px; text-transform: uppercase;
    padding: 5px 12px; border-radius: 3px; margin-top: auto;
  }

  .how-card-left.sky   .how-card-tag { background: rgba(92,143,166,0.12); color: var(--sc-current); border: 1px solid rgba(92,143,166,0.25); }
  .how-card-left.green .how-card-tag { background: rgba(74,124,94,0.1);   color: var(--sc-pine);    border: 1px solid rgba(74,124,94,0.22); }
  .how-card-left.wheat .how-card-tag { background: rgba(201,169,110,0.1); color: #7A6030;           border: 1px solid rgba(201,169,110,0.28); }

  .how-card-right { padding: 40px 44px; display: flex; flex-direction: column; gap: 16px; }
  .how-card-body  { font-size: 15px; color: var(--sc-muted); line-height: 1.9; }

  .how-card-detail {
    font-size: 13.5px; font-family: 'DM Sans', sans-serif; font-style: italic;
    color: var(--sc-quiet); line-height: 1.75;
    padding-top: 14px; border-top: 1px solid var(--sc-border);
  }

  /* ═══════════════════════════════════════════════════
     OVERVIEW: SERVE GRID
  ═══════════════════════════════════════════════════ */
  .serve-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 18px; margin-top: 48px; }

  .serve-card { border-radius: 14px; padding: 36px 30px 32px; border: 1px solid transparent; }

  .serve-card.sky   { background: rgba(92,143,166,0.06);  border-color: rgba(92,143,166,0.18); }
  .serve-card.green { background: rgba(74,124,94,0.05);   border-color: rgba(74,124,94,0.17); }
  .serve-card.wheat { background: rgba(201,169,110,0.06); border-color: rgba(201,169,110,0.2); }
  .serve-card em    { font-style: italic; color: var(--sc-current); }

  .serve-num {
    font-family: 'Lora', serif; font-size: 13px; font-style: italic;
    margin-bottom: 14px; display: block;
  }

  .serve-card.sky   .serve-num { color: var(--sc-sky); }
  .serve-card.green .serve-num { color: var(--sc-fern); }
  .serve-card.wheat .serve-num { color: var(--sc-clay); }

  .serve-card h3 {
    font-family: 'Lora', serif; font-size: 21px; font-weight: 500;
    color: var(--sc-ink); margin-bottom: 12px; line-height: 1.3;
  }

  .serve-card p { font-size: 14px; color: var(--sc-muted); line-height: 1.85; }

  /* ═══════════════════════════════════════════════════
     OVERVIEW: PROTO + STATS
  ═══════════════════════════════════════════════════ */
  .proto-strip {
    display: grid; grid-template-columns: 1fr 1fr 1fr 1fr;
    gap: 1px; background: var(--sc-border);
    border-radius: 14px; overflow: hidden; margin-top: 48px;
  }

  .proto-block { background: white; padding: 36px 30px; position: relative; }
  .proto-block:first-child { border-radius: 14px 0 0 14px; }
  .proto-block:last-child  { border-radius: 0 14px 14px 0; }

  .proto-period {
    font-size: 10px; font-weight: 600; letter-spacing: 2.5px;
    text-transform: uppercase; color: var(--sc-quiet); margin-bottom: 10px; display: block;
  }

  .proto-block h4 {
    font-family: 'Lora', serif; font-size: 17px; font-weight: 500;
    color: var(--sc-ink); margin-bottom: 10px; line-height: 1.3;
  }

  .proto-block p  { font-size: 13px; color: var(--sc-muted); line-height: 1.75; }

  .proto-dot {
    width: 8px; height: 8px; border-radius: 50%;
    position: absolute; top: 36px; right: 30px;
  }

  .proto-dot.q1 { background: var(--sc-current); }
  .proto-dot.q2 { background: var(--sc-sky); }
  .proto-dot.q3 { background: var(--sc-fern); }
  .proto-dot.q4 { background: var(--sc-wheat); }

  .stat-row { display: grid; grid-template-columns: repeat(3,1fr); gap: 18px; margin-top: 48px; }

  .stat-block {
    background: white; border: 1px solid var(--sc-border);
    border-radius: 14px; padding: 36px 30px; position: relative; overflow: hidden;
  }

  .stat-block::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
  }

  .stat-block.sky::before   { background: var(--sc-sky); }
  .stat-block.green::before { background: var(--sc-fern); }
  .stat-block.wheat::before { background: var(--sc-wheat); }

  .stat-num {
    font-family: 'Lora', serif; font-size: 50px; font-weight: 400;
    line-height: 1; letter-spacing: -0.02em; margin-bottom: 10px;
  }

  .stat-block.sky   .stat-num { color: var(--sc-current); }
  .stat-block.green .stat-num { color: var(--sc-fern); }
  .stat-block.wheat .stat-num { color: var(--sc-clay); }

  .stat-label { font-size: 14px; font-weight: 600; color: var(--sc-ink); margin-bottom: 6px; }
  .stat-desc  { font-size: 13px; color: var(--sc-quiet); line-height: 1.7; }

  /* ═══════════════════════════════════════════════════
     OVERVIEW: PULLQUOTE + CLOSE
  ═══════════════════════════════════════════════════ */
  .pullquote {
    margin: 52px 0; padding: 40px 48px;
    border-left: 3px solid var(--sc-sky);
    background: rgba(92,143,166,0.045);
    border-radius: 0 14px 14px 0; max-width: 680px;
  }

  .pullquote blockquote {
    font-family: 'Lora', serif; font-size: 23px; font-weight: 400;
    font-style: italic; line-height: 1.58; color: var(--sc-ink);
  }

  .close-inner {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 56px; align-items: start;
  }

  .close-left h3 {
    font-family: 'Lora', serif; font-size: 26px; font-weight: 400;
    color: #F6FAFB; line-height: 1.4; margin-bottom: 20px; letter-spacing: -0.01em;
  }

  .close-left p { font-size: 15px; color: var(--sc-horizon); line-height: 1.9; margin-bottom: 16px; }

  .close-right {
    background: rgba(133,180,200,0.06); border: 1px solid rgba(133,180,200,0.15);
    border-radius: 14px; padding: 38px 36px; display: flex; flex-direction: column; gap: 20px;
  }

  .close-right-head {
    font-family: 'Lora', serif; font-size: 18px; font-weight: 500;
    color: #F6FAFB; line-height: 1.4; margin-bottom: 4px;
  }

  .close-item {
    display: flex; gap: 14px; padding-top: 18px;
    border-top: 1px solid rgba(133,180,200,0.1);
  }

  .close-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; margin-top: 7px; }
  .close-dot.sky   { background: var(--sc-sky); }
  .close-dot.green { background: var(--sc-moss); }
  .close-dot.wheat { background: var(--sc-wheat); }

  .close-item-text { font-size: 14px; color: var(--sc-horizon); line-height: 1.8; }
  .close-item-text strong { color: #F6FAFB; font-weight: 500; display: block; margin-bottom: 2px; }

  /* ═══════════════════════════════════════════════════
     CALENDAR SECTION
  ═══════════════════════════════════════════════════ */
  .cal-controls {
    background: white; border-bottom: 1px solid var(--sc-border);
    padding: 18px 72px;
    box-shadow: 0 2px 12px rgba(26,46,59,0.05);
  }

  .cal-controls-inner {
    max-width: 1060px; margin: 0 auto;
    display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
  }

  .cal-controls-label {
    font-size: 10px; font-weight: 600; letter-spacing: 3px;
    text-transform: uppercase; color: var(--sc-quiet);
    white-space: nowrap; margin-right: 4px;
  }

  .filter-btn {
    font-family: 'Inter', sans-serif; font-size: 12px; font-weight: 500;
    padding: 6px 14px; border-radius: 4px;
    border: 1px solid var(--sc-border); background: var(--sc-snow);
    color: var(--sc-muted); cursor: pointer;
    transition: all 0.15s ease; white-space: nowrap;
  }

  .filter-btn:hover { border-color: var(--sc-sky); color: var(--sc-current); }
  .filter-btn.active { background: var(--sc-current); border-color: var(--sc-current); color: white; }

  .filter-sep { width: 1px; height: 18px; background: var(--sc-border); flex-shrink: 0; }

  .cal-body { max-width: 1060px; margin: 0 auto; padding: 48px 72px 72px; }

  .month-group { margin-bottom: 44px; }

  .month-heading {
    display: flex; align-items: center; gap: 16px; margin-bottom: 16px;
  }

  .month-heading h3 {
    font-family: 'Lora', serif; font-size: 20px; font-weight: 400;
    color: var(--sc-ink); letter-spacing: -0.01em;
  }

  .month-heading-line { flex: 1; height: 1px; background: var(--sc-border); }
  .month-count { font-size: 11px; font-weight: 500; color: var(--sc-quiet); white-space: nowrap; }

  .events-list { display: flex; flex-direction: column; gap: 8px; }

  .event-card {
    display: grid; grid-template-columns: 64px 130px 120px 1fr auto;
    align-items: center; background: white;
    border: 1px solid var(--sc-border); border-radius: 10px;
    overflow: hidden; transition: box-shadow 0.15s ease, transform 0.15s ease;
  }

  .event-card:hover { box-shadow: 0 4px 20px rgba(26,46,59,0.08); transform: translateY(-1px); }
  .event-card[data-visible="false"] { display: none; }

  .ev-date {
    padding: 16px 12px; text-align: center;
    border-right: 1px solid var(--sc-border); flex-shrink: 0;
  }

  .ev-date-day {
    font-family: 'Lora', serif; font-size: 24px; font-weight: 400;
    color: var(--sc-current); line-height: 1; display: block;
  }

  .ev-date-dow {
    font-size: 9px; font-weight: 600; letter-spacing: 1.5px;
    text-transform: uppercase; color: var(--sc-quiet); display: block; margin-top: 3px;
  }

  .ev-region {
    padding: 0 14px; border-right: 1px solid var(--sc-border);
    display: flex; align-items: center; justify-content: center; height: 100%;
  }

  .region-pill {
    font-size: 9px; font-weight: 600; letter-spacing: 1.5px;
    text-transform: uppercase; padding: 4px 10px; border-radius: 3px; white-space: nowrap;
  }

  .region-sg   { background: var(--sc-cloud);  color: var(--sc-current); border: 1px solid rgba(92,143,166,0.22); }
  .region-hk   { background: #E8F4F8;           color: #2A4558;           border: 1px solid rgba(42,69,88,0.18); }
  .region-my   { background: var(--sc-meadow);  color: var(--sc-fern);    border: 1px solid rgba(74,124,94,0.2); }
  .region-usa  { background: #F5EFE6;           color: var(--sc-clay);    border: 1px solid rgba(160,128,96,0.22); }
  .region-uk   { background: #F5EFE6;           color: var(--sc-clay);    border: 1px solid rgba(160,128,96,0.22); }
  .region-glob { background: var(--sc-breath);  color: var(--sc-muted);   border: 1px solid var(--sc-border); }
  .region-multi{ background: var(--sc-breath);  color: var(--sc-current); border: 1px solid rgba(92,143,166,0.2); }

  .ev-type {
    padding: 0 14px; border-right: 1px solid var(--sc-border);
    display: flex; align-items: center; justify-content: center; height: 100%;
  }

  .type-badge {
    font-size: 9px; font-weight: 600; letter-spacing: 1.5px;
    text-transform: uppercase; color: var(--sc-quiet);
    padding: 4px 9px; border-radius: 3px;
    background: var(--sc-snow); border: 1px solid var(--sc-border); white-space: nowrap;
  }

  .ev-body { padding: 14px 20px; }

  .ev-name {
    font-size: 13.5px; font-weight: 600; color: var(--sc-ink);
    margin-bottom: 2px; line-height: 1.4;
  }

  .ev-meta {
    font-family: 'DM Sans', sans-serif; font-size: 11.5px;
    font-style: italic; color: var(--sc-quiet); line-height: 1.5;
  }

  .ev-org { padding: 0 16px; display: flex; align-items: center; height: 100%; }

  .org-tag {
    font-size: 10px; font-weight: 500; color: var(--sc-muted);
    background: var(--sc-breath); border: 1px solid var(--sc-border);
    border-radius: 3px; padding: 3px 9px; white-space: nowrap;
  }

  .empty-state { padding: 56px 32px; text-align: center; display: none; }
  .empty-state.visible { display: block; }
  .empty-state p { font-family: 'Lora', serif; font-size: 18px; font-style: italic; color: var(--sc-quiet); }

  /* ═══════════════════════════════════════════════════
     RESOURCES SECTION
  ═══════════════════════════════════════════════════ */
  .res-body { max-width: 1060px; margin: 0 auto; padding: 64px 72px 80px; }

  .res-category { margin-bottom: 52px; }

  .res-category-heading {
    display: flex; align-items: center; gap: 16px; margin-bottom: 24px;
  }

  .res-category-heading h3 {
    font-family: 'Lora', serif; font-size: 22px; font-weight: 400;
    color: var(--sc-ink); letter-spacing: -0.01em;
  }

  .res-category-line { flex: 1; height: 1px; background: var(--sc-border); }

  .res-category-count {
    font-size: 11px; font-weight: 500; color: var(--sc-quiet); white-space: nowrap;
  }

  .res-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 16px; }

  .res-card {
    background: white; border: 1px solid var(--sc-border);
    border-radius: 12px; padding: 24px 26px;
    display: flex; flex-direction: column; gap: 10px;
    transition: box-shadow 0.15s ease, transform 0.15s ease;
    text-decoration: none;
    position: relative; overflow: hidden;
  }

  .res-card:hover { box-shadow: 0 4px 20px rgba(26,46,59,0.08); transform: translateY(-2px); }

  .res-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
  }

  .res-card.ministry::before { background: var(--sc-sky); }
  .res-card.books::before    { background: var(--sc-wheat); }
  .res-card.podcast::before  { background: var(--sc-fern); }

  .res-card-name {
    font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600;
    color: var(--sc-ink); line-height: 1.4;
  }

  .res-card-link {
    font-size: 12px; color: var(--sc-sky); text-decoration: none;
    word-break: break-all; line-height: 1.5;
    display: flex; align-items: center; gap: 5px;
  }

  .res-card-link::after {
    content: '↗'; font-size: 11px; flex-shrink: 0;
  }

  .res-card-contact {
    font-size: 11.5px; color: var(--sc-muted); line-height: 1.6;
    padding-top: 10px; border-top: 1px solid var(--sc-border);
    font-family: 'DM Sans', sans-serif;
  }

  .res-card-contact strong { color: var(--sc-ink); font-weight: 500; }

  /* ═══════════════════════════════════════════════════
     FOOTER
  ═══════════════════════════════════════════════════ */
  .footer {
    background: var(--sc-deep); border-top: 1px solid rgba(133,180,200,0.1);
    padding: 48px 72px; display: flex; align-items: center;
    justify-content: space-between; flex-wrap: wrap; gap: 20px;
  }

  .wordmark-name {
    font-family: 'Lora', serif; font-size: 22px; font-weight: 400;
    color: #F6FAFB; letter-spacing: 0.01em; display: block;
  }

  .wordmark-sub {
    font-size: 9px; font-weight: 600; letter-spacing: 4px;
    text-transform: uppercase; color: var(--sc-horizon);
    display: block; margin-top: 3px;
  }

  .footer-contact { text-align: right; font-size: 13px; color: var(--sc-muted); line-height: 1.75; }
  .footer-contact a { color: var(--sc-horizon); text-decoration: none; }

  /* ═══════════════════════════════════════════════════
     MINISTRIES DIRECTORY
  ═══════════════════════════════════════════════════ */
  .min-controls {
    background: white; border-bottom: 1px solid var(--sc-border);
    padding: 18px 72px; box-shadow: 0 2px 12px rgba(26,46,59,0.05);
  }
  .min-controls-inner {
    max-width: 1060px; margin: 0 auto;
    display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
  }
  .min-search-wrap { position: relative; flex: 1; min-width: 200px; }
  .min-search-wrap svg {
    position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
    width: 15px; height: 15px; pointer-events: none; color: var(--sc-quiet);
  }
  .min-search {
    width: 100%; height: 36px; padding: 0 14px 0 36px;
    font-family: 'Inter', sans-serif; font-size: 13px;
    border: 1px solid var(--sc-border); border-radius: 4px;
    background: var(--sc-snow); color: var(--sc-ink); outline: none;
    transition: border-color 0.15s;
  }
  .min-search::placeholder { color: var(--sc-quiet); }
  .min-search:focus { border-color: var(--sc-sky); }
  .min-filter-sep { width: 1px; height: 18px; background: var(--sc-border); flex-shrink: 0; }
  .min-filter-btn {
    font-family: 'Inter', sans-serif; font-size: 12px; font-weight: 500;
    padding: 6px 14px; border-radius: 4px;
    border: 1px solid var(--sc-border); background: var(--sc-snow);
    color: var(--sc-muted); cursor: pointer;
    transition: all 0.15s ease; white-space: nowrap;
  }
  .min-filter-btn:hover { border-color: var(--sc-sky); color: var(--sc-current); }
  .min-filter-btn.active { background: var(--sc-current); border-color: var(--sc-current); color: white; }

  .min-body { max-width: 1060px; margin: 0 auto; padding: 40px 72px 72px; }
  .min-results-meta { font-size: 12px; color: var(--sc-quiet); margin-bottom: 20px; }

  .min-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 18px;
  }
  .min-card {
    background: white; border: 1px solid var(--sc-border);
    border-radius: 12px; padding: 24px 26px;
    display: flex; flex-direction: column; gap: 10px;
    transition: box-shadow 0.15s ease, transform 0.15s ease;
  }
  .min-card:hover { box-shadow: 0 4px 20px rgba(26,46,59,0.08); transform: translateY(-2px); }
  .min-card-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; }
  .min-card-name {
    font-family: 'Lora', serif; font-size: 15px; font-weight: 500;
    color: var(--sc-ink); line-height: 1.3; flex: 1;
  }
  .min-type-pill {
    font-size: 9px; font-weight: 600; letter-spacing: 1.5px;
    text-transform: uppercase; padding: 4px 10px; border-radius: 3px;
    white-space: nowrap; flex-shrink: 0;
  }
  .min-programme { background: var(--sc-meadow); color: var(--sc-pine); border: 1px solid rgba(45,92,62,0.18); }
  .min-conference { background: var(--sc-cloud);  color: var(--sc-current); border: 1px solid rgba(92,143,166,0.22); }
  .min-card-desc { font-size: 13px; color: var(--sc-muted); line-height: 1.75; }
  .min-tags { display: flex; flex-wrap: wrap; gap: 5px; }
  .min-tag {
    font-size: 10px; font-weight: 500; padding: 3px 9px;
    border-radius: 3px; background: var(--sc-breath);
    color: var(--sc-muted); border: 1px solid var(--sc-border);
  }
  .min-meta {
    display: grid; grid-template-columns: repeat(3,1fr); gap: 6px;
    padding: 10px 0; border-top: 1px solid var(--sc-border); border-bottom: 1px solid var(--sc-border);
  }
  .min-meta-cell { font-size: 10px; font-weight: 600; letter-spacing: 1px; text-transform: uppercase; color: var(--sc-quiet); }
  .min-meta-cell strong { display: block; font-size: 13px; font-weight: 500; color: var(--sc-ink); letter-spacing: 0; text-transform: none; margin-top: 2px; }
  .min-card-footer { display: flex; justify-content: flex-end; align-items: center; }
  .min-card-link {
    font-size: 12px; font-weight: 500; color: var(--sc-sky);
    text-decoration: none; display: inline-flex; align-items: center; gap: 4px;
    transition: gap 0.15s;
  }
  .min-card-link:hover { gap: 8px; color: var(--sc-current); }
  .min-card-link::after { content: '↗'; font-size: 11px; }
  .min-no-link { font-size: 12px; color: var(--sc-quiet); font-style: italic; }
  .min-empty { padding: 56px 32px; text-align: center; }
  .min-empty p { font-family: 'Lora', serif; font-size: 18px; font-style: italic; color: var(--sc-quiet); }

  /* ═══════════════════════════════════════════════════
     RESPONSIVE
  ═══════════════════════════════════════════════════ */
  @media (max-width: 900px) {
    .site-nav { padding: 0 20px; }
    .nav-link { padding: 7px 10px; font-size: 11px; }
    .hero { grid-template-columns: 1fr; }
    .hero-right { border-left: none; border-top: 1px solid rgba(133,180,200,0.12); padding: 48px 28px 80px; min-height: auto; }
    .hero-left  { padding: 80px 28px 48px; min-height: 60vh; }
    .section { padding: 60px 24px; }
    .section-header { padding: 52px 24px 36px; }
    .section-header-inner { flex-direction: column; align-items: flex-start; gap: 16px; }
    .devotional { padding: 60px 24px; }
    .scripture-card { padding: 28px 24px; }
    .tension-split { grid-template-columns: 1fr; }
    .tension-divider { display: none; }
    .how-card { grid-template-columns: 1fr; }
    .how-card-left { border-right: none; border-bottom: 1px solid var(--sc-border); }
    .serve-grid { grid-template-columns: 1fr; }
    .proto-strip { grid-template-columns: 1fr 1fr; }
    .stat-row { grid-template-columns: 1fr; }
    .close-inner { grid-template-columns: 1fr; gap: 32px; }
    .cal-controls { padding: 14px 24px; }
    .cal-body { padding: 32px 24px 60px; }
    .event-card { grid-template-columns: 56px 1fr; grid-template-rows: auto auto auto; }
    .ev-region, .ev-type, .ev-org { border: none; padding: 0; justify-content: flex-start; }
    .ev-region { padding: 8px 14px 0; }
    .ev-type   { padding: 4px 14px 0; }
    .ev-body   { padding: 6px 14px 10px; grid-column: 1 / -1; }
    .ev-org    { padding: 0 14px 10px; grid-column: 2; }
    .ev-date   { grid-row: 1 / 3; border-right: 1px solid var(--sc-border); }
    .res-body  { padding: 40px 24px 60px; }
    .res-cards { grid-template-columns: 1fr; }
    .min-controls { padding: 14px 24px; }
    .min-body { padding: 28px 24px 60px; }
    .min-grid { grid-template-columns: 1fr; }
    .footer    { padding: 36px 24px; flex-direction: column; align-items: flex-start; }
    .footer-contact { text-align: left; }
  }
</style>
</head>
<body>

<!-- ═══ STICKY NAV ══════════════════════════════════════════════ -->
<nav class="site-nav">
  <a class="nav-wordmark" href="#overview">
    <span class="nav-wordmark-name">Stewardship</span>
    <span class="nav-wordmark-sub">Collective</span>
  </a>
  <div class="nav-links">
    <a class="nav-link active" href="#overview" onclick="setActive(this)">Overview</a>
    <div class="nav-sep"></div>
    <a class="nav-link" href="#calendar" onclick="setActive(this)">Calendar</a>
    <div class="nav-sep"></div>
    <a class="nav-link" href="#resources" onclick="setActive(this)">Resources</a>
    <div class="nav-sep"></div>
    <a class="nav-link" href="#ministries" onclick="setActive(this)">Ministries</a>
  </div>
</nav>

<!-- ══════════════════════════════════════════════════════════════
     SECTION 1: OVERVIEW
══════════════════════════════════════════════════════════════ -->
<div id="overview">

<!-- HERO -->
<section class="hero">
  <div class="hero-orb"></div>
  <div class="hero-orb2"></div>
  <div class="hero-left">
    <span class="overline">Stewardship Collective · Singapore</span>
    <h1>Connecting<br>resources<br>with <em>relationship.</em></h1>
  </div>
  <div class="hero-right">
    <span class="hero-right-label">Who We Are</span>
    <p class="hero-descriptor">"A flourishing community reimagining, and joyfully stewarding His resources, to participate in Christ's redemptive work."</p>
    <p class="hero-body">Stewardship Collective is a faith-based intermediary building trusted community between Christian wealth stewards and impact partners across Singapore and Southeast Asia. We are a convener, a bridge builder, and a community-maker — not a fund, not a platform.</p>
  </div>
</section>

<!-- DEVOTIONAL -->
<section class="devotional">
<div class="devotional-inner">
  <span class="devotional-label">A Word to Begin</span>
  <div class="scripture-card">
    <span class="scripture-ref">Philippians 2:5–11 · ESV</span>
    <ul class="scripture-verses">
      <li class="scripture-verse"><span class="verse-num">5</span><span class="verse-text"><em>Have this mind among yourselves, which is yours in Christ Jesus,</em></span></li>
      <li class="scripture-verse"><span class="verse-num">6</span><span class="verse-text">who, though he was in the form of God, did not count equality with God a thing to be grasped,</span></li>
      <li class="scripture-verse"><span class="verse-num">7</span><span class="verse-text">but emptied himself, by taking the form of a servant, being born in the likeness of men.</span></li>
      <li class="scripture-verse"><span class="verse-num">8</span><span class="verse-text">And being found in human form, he humbled himself by becoming obedient to the point of death, even death on a cross.</span></li>
      <li class="scripture-verse"><span class="verse-num">9</span><span class="verse-text">Therefore God has highly exalted him and bestowed on him the name that is above every name,</span></li>
      <li class="scripture-verse"><span class="verse-num">10</span><span class="verse-text">so that at the name of Jesus every knee should bow, in heaven and on earth and under the earth,</span></li>
      <li class="scripture-verse"><span class="verse-num">11</span><span class="verse-text">and every tongue confess that Jesus Christ is Lord, to the glory of God the Father.</span></li>
    </ul>
  </div>
  <p class="devotional-body">As Christians, all our lives are lived in response to this profound act of humility and grace. In every season — in want and in plenty — we return to the image of the humble servant who emptied himself for us. That posture shapes everything: how we hold what we have, how we give, and how we show up for one another.</p>
  <p class="devotional-body">We continue to wrestle with the immense brokenness in this world. And yet we also see, again and again, that God's redemptive plan is constantly at work — moving through ordinary people, in ordinary places, doing ordinary things faithfully. He invites us to participate joyfully and sacrificially in the diverse roles He has called each of us to play.</p>
  <p class="devotional-body">Stewardship Collective was born from this tension — the tension between what is and what could be, between the brokenness we see and the redemption we trust is coming. It is our prayerful, humble attempt to propose one way forward for believers who want to hold their resources more faithfully, more relationally, and more joyfully.</p>
  <p class="devotional-closing">We offer this work not as an answer, but as an invitation to discern together — with open hands.</p>
</div>
</section>

<!-- THE PROBLEM -->
<section class="section tinted">
<div class="inner">
  <span class="label">The Problem We Are Responding To</span>
  <h2>God's resources are here.<br>The conditions to deploy them <em>faithfully</em> are not.</h2>
  <p class="body-text">Across Singapore and Southeast Asia, there is no shortage of generosity or goodwill. What is missing is the trusted, relational infrastructure that allows resources to flow from the right hands to the right places — in ways that honour both the giver and the receiver.</p>
  <div class="tension-split">
    <div class="tension-side">
      <div class="tension-icon sky">
        <svg viewBox="0 0 24 24" fill="none"><path d="M12 12C13.66 12 15 10.66 15 9C15 7.34 13.66 6 12 6C10.34 6 9 7.34 9 9C9 10.66 10.34 12 12 12ZM12 14C9.99 14 6 15.01 6 17V18H18V17C18 15.01 14.01 14 12 14Z" fill="#5C8FA6"/></svg>
      </div>
      <span class="tension-side-label sky">The Steward</span>
      <h3>Wanting to steward well. Often journeying alone.</h3>
      <ul class="tension-list">
        <li>Stewardship — giving, investing, time, influence — often happens in isolation, without trusted peers</li>
        <li>Biblical formation around money, wealth, and investment is fragmented or absent from community life</li>
        <li>Limited awareness of how to integrate faith with financial decision-making, philanthropy, and impact investing</li>
        <li>Generosity can feel transactional; investing can feel disconnected from values — neither feels fully integrated</li>
        <li>Wealth-holders rarely have safe spaces to surface doubt, wrestle with abundance, or discern together</li>
      </ul>
    </div>
    <div class="tension-divider"><div class="gap-pill"><span class="gap-pill-text">The Gap</span></div></div>
    <div class="tension-side">
      <div class="tension-icon green">
        <svg viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7L12 12L22 7L12 2Z" fill="#4A7C5E" opacity="0.4"/><path d="M2 17L12 22L22 17M2 12L12 17L22 12" stroke="#4A7C5E" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none"/></svg>
      </div>
      <span class="tension-side-label green">The Partner</span>
      <h3>Doing important work. Chronically under-resourced.</h3>
      <ul class="tension-list">
        <li>Funding arrives without the strategic or relational support to use it well</li>
        <li>Leaders burn out managing donor relationships alongside their core work</li>
        <li>Significant human and financial capital remains unmobilised across SEA</li>
        <li>No trusted intermediary connects both worlds with integrity and care</li>
      </ul>
    </div>
  </div>
</div>
</section>

<!-- VISION -->
<section class="section">
<div class="inner-narrow">
  <span class="label">Our Vision &amp; Role</span>
  <h2>We exist to close the gap — not with a programme, but with <em>a community.</em></h2>
  <p class="body-text">Stewardship Collective cultivates a Christ-centred community that reimagines how God's people steward His resources. We believe stewardship is not merely about allocation — it is about formation. Resources should flow from relationships. Capital should be paired with care. And giving should be joyful, not cautious.</p>
  <div class="pullquote">
    <blockquote>"We want to be excellent stewards — rigorous, accountable, wise. And we want to give and invest with joy, trust, and open hands. The tragedy is that these so rarely come together."</blockquote>
  </div>
  <p class="body-text">We are not another fund or giving platform. We are a convener — bringing stewards and partners into genuine, trusted relationship. A bridge builder — creating pathways for resources to flow wisely and well. A community-maker — because the real work of stewardship happens in relationship, not in isolation.</p>
</div>
</section>

<!-- HOW WE WORK -->
<section class="section sky-tint">
<div class="inner">
  <span class="label">How We Work</span>
  <h2>Three streams. <em>One integrated model.</em></h2>
  <p class="body-text">Everything SC does flows through three interconnected streams. They are not separate programmes — they are one way of working, each stream reinforcing the others.</p>
  <div class="how-cards">
    <div class="how-card">
      <div class="how-card-left sky">
        <div><div class="how-card-num">I</div><div class="how-card-title">Trusted Communities of Stewards</div></div>
        <span class="how-card-tag">Stewardship Circles</span>
      </div>
      <div class="how-card-right">
        <p class="how-card-body">We convene small, cause-aligned circles where stewards can share openly — including their joys, questions, and longings around giving — and learn from one another as peers. Formation comes before activation. Relationship comes before transaction.</p>
        <p class="how-card-body">These are not networking events or giving programmes. They are ongoing communities of practice — spaces for honest journeying together, grounded in Scripture and shaped by trust.</p>
        <p class="how-card-detail">Monthly gatherings, discipleship-focused, built around a shared sense of call and cause.</p>
      </div>
    </div>
    <div class="how-card">
      <div class="how-card-left green">
        <div><div class="how-card-num">II</div><div class="how-card-title">Rigorous Deployment of Capital</div></div>
        <span class="how-card-tag">Stewardship Pathways</span>
      </div>
      <div class="how-card-right">
        <p class="how-card-body">We create professional, excellent pathways for stewardship in its fullest sense — encompassing pooled giving, impact investing, and every point along the spectrum between. Resources are channelled through vetted, well-assessed partner initiatives.</p>
        <p class="how-card-body">Whether a steward is deploying through philanthropy, faith-driven investment, or hybrid structures, every pathway is designed to honour both the steward's intent and the partner's dignity. Resources flow from relationship, not from a transaction.</p>
        <p class="how-card-detail">Partners are assessed for trustworthiness, need, and readiness — not just programmatic outputs. Pathways span giving, impact investing, and blended finance.</p>
      </div>
    </div>
    <div class="how-card">
      <div class="how-card-left wheat">
        <div><div class="how-card-num">III</div><div class="how-card-title">Capacity Building Beyond the Cheque</div></div>
        <span class="how-card-tag">Partner Capacity</span>
      </div>
      <div class="how-card-right">
        <p class="how-card-body">Strategic, relational, and practical support for on-the-ground partners — going far beyond funding. We walk alongside organisations in leadership development, organisational systems, fundraising capability, and long-term accompaniment.</p>
        <p class="how-card-body">Partners are not recipients of charity. They are collaborators in the same redemptive work. Our support is designed to strengthen their capacity to flourish — long after our involvement has ended.</p>
        <p class="how-card-detail">Support is holistic and relational — not transactional or time-limited to a grant cycle.</p>
      </div>
    </div>
  </div>
</div>
</section>

<!-- WHO WE SERVE + PROTOTYPE -->
<section class="section tinted">
<div class="inner">
  <span class="label">Who We Serve</span>
  <h2>Four communities.<br><em>One ecosystem.</em></h2>
  <p class="body-text">SC sits at the intersection of several communities, each with a distinct role in the stewardship ecosystem we are trying to build. We are not trying to replace what exists — we are trying to connect, complement, and strengthen it.</p>
  <div class="serve-grid">
    <div class="serve-card sky">
      <span class="serve-num">For stewards</span>
      <h3>Those who hold resources and want to steward them well</h3>
      <p>Individuals, families, and organisations with financial, relational, or organisational capacity. People who want to give, invest, and deploy resources faithfully — looking for the community, formation, and trusted pathways to do so with integrity, rigour, and joy.</p>
    </div>
    <div class="serve-card green">
      <span class="serve-num">For impact partners</span>
      <h3>Organisations doing justice and mercy work in SEA</h3>
      <p>Nonprofits, ministries, and social enterprises working on the ground across Singapore and Southeast Asia. Organisations that need more than funding — they need strategic accompaniment, capacity support, and genuine relationship with those who give and invest alongside them.</p>
    </div>
    <div class="serve-card wheat">
      <span class="serve-num">For the wider church</span>
      <h3>The body of Christ, learning to hold things more loosely</h3>
      <p>Churches, pastors, and Christian communities wrestling with what faithful stewardship looks like in a context of growing wealth. We provide teaching, exposure journeys, and pathways for the broader church to participate in this work — not as spectators, but as active stewards.</p>
    </div>
    <div class="serve-card" style="background:rgba(61,100,120,0.05);border:1px solid rgba(61,100,120,0.18);">
      <span class="serve-num" style="color:var(--sc-current);">For ecosystem partners</span>
      <h3>Stewardship ministries already doing this work</h3>
      <p>Organisations like <em>Journey of Generosity</em>, <em>Praxis</em>, <em>Faith Driven Investor</em>, <em>N5</em>, and <em>Generosity Path</em> are already forming stewards and building capacity. SC exists to connect, complement, and collaborate — not compete.</p>
    </div>
  </div>

  <div style="margin-top:64px;">
    <span class="label">Where We Are Now</span>
    <h2 style="margin-bottom:16px;">Prototype phase, 2026–2027.</h2>
    <p class="body-text">We are starting small and intentionally. Trust must be cultivated, not assumed — and a model for Singapore and SEA must be learned contextually, not imported. Our 24-month prototype tests the model across three Stewardship Circles, three partner organisations, and a documented learning framework.</p>
  </div>

  <div class="proto-strip">
    <div class="proto-block"><div class="proto-dot q1"></div><span class="proto-period">Q1–Q2 2026</span><h4>Form &amp; Listen</h4><p>Convene the first communities. Build trust through story and honest conversation. Relationship before activity.</p></div>
    <div class="proto-block"><div class="proto-dot q2"></div><span class="proto-period">Q3–Q4 2026</span><h4>Activate &amp; Fund</h4><p>First pooled giving round. Begin partner capacity support. The community moves from formation into action.</p></div>
    <div class="proto-block"><div class="proto-dot q3"></div><span class="proto-period">Q1–Q2 2027</span><h4>Deepen &amp; Learn</h4><p>Deepen the circles. Document what works contextually. Strengthen partner relationships.</p></div>
    <div class="proto-block"><div class="proto-dot q4"></div><span class="proto-period">Q3–Q4 2027</span><h4>Evaluate &amp; Shape</h4><p>Produce the Stewardship &amp; Capacity Framework. Discern together what comes next.</p></div>
  </div>

  <div class="stat-row">
    <div class="stat-block sky"><div class="stat-num">3</div><div class="stat-label">Stewardship Circles</div><div class="stat-desc">High-trust communities of stewards formed around shared causes — cause-aligned, formation-focused, relationally deep.</div></div>
    <div class="stat-block green"><div class="stat-num">3</div><div class="stat-label">Partners Strengthened</div><div class="stat-desc">Organisations supported holistically — funding paired with strategic, relational, and capacity-building support.</div></div>
    <div class="stat-block wheat"><div class="stat-num">$150k+</div><div class="stat-label">Capital Deployed</div><div class="stat-desc">Seed capital channelled into vetted partner initiatives through the Stewardship Circles. Resources in relationship.</div></div>
  </div>
</div>
</section>

<!-- GET IN TOUCH -->
<section class="section dark">
<div class="inner">
  <span class="label label-light">Get in Touch</span>
  <div class="close-inner">
    <div class="close-left">
      <h3>We are a small team building something new. We'd love to hear from you.</h3>
      <p>Whether you're a steward curious about what faithful giving looks like in community, a partner wondering if SC might be a good fit, or a church leader thinking about how to resource your congregation's generosity — we want to have the conversation.</p>
      <p>This work is relational at its core. So every inquiry starts the same way: with a real conversation, not a form.</p>
    </div>
    <div class="close-right">
      <div class="close-right-head">How to connect with us</div>
      <div class="close-item">
        <div class="close-dot sky"></div>
        <div class="close-item-text"><strong>Write to us</strong>darrellong92@gmail.com — We respond personally to every message and will find a time to talk.</div>
      </div>
      <div class="close-item">
        <div class="close-dot green"></div>
        <div class="close-item-text"><strong>Refer someone</strong>Know a steward, leader, or partner who should be part of this conversation? Connect us directly or share this page.</div>
      </div>
      <div class="close-item">
        <div class="close-dot wheat"></div>
        <div class="close-item-text"><strong>Pray for the work</strong>We believe this is first and foremost an act of faith. If you're not ready to engage but want to carry this in prayer — that matters deeply to us.</div>
      </div>
    </div>
  </div>
</div>
</section>

</div><!-- /overview -->

<!-- ══════════════════════════════════════════════════════════════
     SECTION 2: CALENDAR
══════════════════════════════════════════════════════════════ -->
<div id="calendar">

<div class="section-header">
  <div class="section-header-inner">
    <h2>Ministry Calendar<br><em>2026</em></h2>
    <p>A shared view of stewardship, giving, and impact-investing events across Singapore and Southeast Asia. Updated by the SC team — write to us if you know of an event that should be listed.</p>
  </div>
</div>

<!-- Calendar filters -->
<div class="cal-controls">
  <div class="cal-controls-inner">
    <span class="cal-controls-label">Filter</span>
    <button class="filter-btn active" onclick="filterAll()">All events</button>
    <div class="filter-sep"></div>
    <button class="filter-btn" onclick="filterMonth(this,'March 2026')">Mar</button>
    <button class="filter-btn" onclick="filterMonth(this,'April 2026')">Apr</button>
    <button class="filter-btn" onclick="filterMonth(this,'May 2026')">May</button>
    <button class="filter-btn" onclick="filterMonth(this,'June 2026')">Jun</button>
    <button class="filter-btn" onclick="filterMonth(this,'July 2026')">Jul</button>
    <button class="filter-btn" onclick="filterMonth(this,'August 2026')">Aug</button>
    <button class="filter-btn" onclick="filterMonth(this,'September 2026')">Sep</button>
    <button class="filter-btn" onclick="filterMonth(this,'October 2026')">Oct</button>
    <button class="filter-btn" onclick="filterMonth(this,'November 2026')">Nov</button>
    <div class="filter-sep"></div>
    <button class="filter-btn" onclick="filterRegion(this,'Singapore')">🇸🇬 Singapore</button>
    <button class="filter-btn" onclick="filterRegion(this,'Hong Kong')">🇭🇰 Hong Kong</button>
    <button class="filter-btn" onclick="filterRegion(this,'Malaysia')">🇲🇾 Malaysia</button>
    <button class="filter-btn" onclick="filterRegion(this,'Overseas')">✈ Overseas</button>
  </div>
</div>

<div class="cal-body">
  <div id="calendar-root"></div>
  <div class="empty-state" id="empty-state"><p>No events found for this filter.</p></div>
</div>

</div><!-- /calendar -->

<!-- ══════════════════════════════════════════════════════════════
     SECTION 3: RESOURCES
══════════════════════════════════════════════════════════════ -->
<div id="resources">

<div class="section-header" style="border-top-color: var(--sc-wheat);">
  <div class="section-header-inner">
    <h2>Stewardship<br><em>Resources</em></h2>
    <p>A curated library of ministries, books, and podcasts for those on a stewardship journey. We add to this as we discover more — write to us if something belongs here.</p>
  </div>
</div>

<div class="res-body">
  <div id="res-body-dynamic">
    <div style="padding:40px 0;color:var(--sc-quiet);font-size:14px;">Loading resources…</div>
  </div>
</div>
</div><!-- /resources -->

<!-- ══════════════════════════════════════════════════════════════
     SECTION 4: MINISTRIES DIRECTORY
══════════════════════════════════════════════════════════════ -->
<div id="ministries">

<div class="section-header" style="border-top-color: var(--sc-fern);">
  <div class="section-header-inner">
    <h2>Stewardship Ministries<br><em>in Singapore</em></h2>
    <p>A curated gateway to Singapore's biblical stewardship and generosity organisations. Not a replacement for their own websites — a first step to help you find where you belong.</p>
  </div>
</div>

<div class="min-controls">
  <div class="min-controls-inner">
    <div class="min-search-wrap">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
      <input class="min-search" type="text" id="min-search" placeholder="Search ministries, topics, audiences…" />
    </div>
    <div class="min-filter-sep"></div>
    <button class="min-filter-btn active" data-minfilter="all">All</button>
    <button class="min-filter-btn" data-minfilter="programme">Programmes</button>
    <button class="min-filter-btn" data-minfilter="conference">Conferences</button>
    <div class="min-filter-sep"></div>
    <button class="min-filter-btn" data-minfilter="finance-professionals">Finance professionals</button>
    <button class="min-filter-btn" data-minfilter="all-believers">All believers</button>
  </div>
</div>

<div class="min-body">
  <div class="min-results-meta" id="min-meta"></div>
  <div class="min-grid" id="min-grid">
    <div style="padding:40px 0;color:var(--sc-quiet);font-size:14px;">Loading ministries…</div>
  </div>
</div>

</div><!-- /ministries -->

<!-- FOOTER -->
<footer class="footer">
  <div>
    <span class="wordmark-name">Stewardship</span>
    <span class="wordmark-sub">Collective</span>
  </div>
  <div class="footer-contact">
    Singapore · Prototype Phase 2026–2027<br>
    <a href="mailto:darrellong92@gmail.com">darrellong92@gmail.com</a>
  </div>
</footer>

<script>
/* ═══════════════════════════════════════════════════════════
   GOOGLE SHEETS CONFIGURATION
   ─────────────────────────────────────────────────────────
   For each sheet:
   1. Open the Google Sheet
   2. File → Share → Publish to the web
   3. Select the correct tab, choose CSV, click Publish
   4. Paste the URL in the variable below

   CALENDAR SHEET columns (row 1 headers):
   date | month | region | name | org | type

   RESOURCES SHEET columns (row 1 headers):
   category | name | url | contact | active
   (category = "ministry", "books", or "podcast")

   MINISTRIES SHEET columns (row 1 headers):
   name | type | description | tags | duration | cost | started | url | audience | active
   (type = "programme" or "conference")
   (audience = comma-separated: "all-believers" and/or "finance-professionals")
   (active = TRUE or FALSE)
═══════════════════════════════════════════════════════════ */

const CALENDAR_CSV_URL   = 'https://docs.google.com/spreadsheets/d/e/2PACX-1vQDnIEgTkb-bXG5nRR4aKbjUtKmGq0f9J_pOandB0mttZ3CEkgd7JuyMs-KXG2u7cWy6YJYYPDQch40/pub?gid=272605304&single=true&output=csv'; // ← paste your calendar sheet CSV URL here
const RESOURCES_CSV_URL  = 'https://docs.google.com/spreadsheets/d/e/2PACX-1vS7GGwNOi9MX8pn_fBVHi8stsf5-0cK4dMuzD2lXuhkJNlSkC9N2rOkijshbtfwcV5kpp7szPxLMk9i/pubhtml?gid=18828155&single=true'; // ← paste your resources sheet CSV URL here
const MINISTRIES_CSV_URL = 'https://docs.google.com/spreadsheets/d/e/2PACX-1vRozK1bsqMi1Z_Yp7VmipkMG_Ec7SySbhy3CuAQuXKLRWJ_hArM2_LVWBmUJnt71tkxPxh51Iurtbh3/pub?gid=295204319&single=true&output=csv'; // ← paste your ministries sheet CSV URL here

/* ── NAV ACTIVE STATE ───────────────────────────────────── */
function setActive(el) {
  document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
  el.classList.add('active');
}

const sections = ['overview','calendar','resources','ministries'];
window.addEventListener('scroll', () => {
  const scrollY = window.scrollY + 80;
  let current = 'overview';
  sections.forEach(id => {
    const el = document.getElementById(id);
    if (el && el.offsetTop <= scrollY) current = id;
  });
  document.querySelectorAll('.nav-link').forEach(l => {
    l.classList.toggle('active', l.getAttribute('href') === '#' + current);
  });
});

/* ── SHARED CSV PARSER ──────────────────────────────────── */
function parseCSV(text) {
  const lines = text.trim().split('\n');
  if (lines.length < 2) return [];
  const headers = lines[0].split(',').map(h => h.trim().replace(/^"|"$/g, '').toLowerCase());
  return lines.slice(1).map(line => {
    const vals = [];
    let inQuote = false, cur = '';
    for (let i = 0; i < line.length; i++) {
      const c = line[i];
      if (c === '"') { inQuote = !inQuote; }
      else if (c === ',' && !inQuote) { vals.push(cur.trim()); cur = ''; }
      else { cur += c; }
    }
    vals.push(cur.trim());
    const obj = {};
    headers.forEach((h, i) => { obj[h] = (vals[i] || '').replace(/^"|"$/g, '').trim(); });
    return obj;
  });
}

/* ═══════════════════════════════════════════════════════════
   SECTION 2: CALENDAR
═══════════════════════════════════════════════════════════ */
const FALLBACK_EVENTS = [
  { date:"2026-03-24", month:"March 2026",     region:"Hong Kong",  name:"Global Investors Symposium Hong Kong",                           org:"",               type:"Conference" },
  { date:"2026-03-26", month:"March 2026",     region:"Hong Kong",  name:"Wealth For Good",                                               org:"",               type:"Conference" },
  { date:"2026-03-27", month:"March 2026",     region:"Hong Kong",  name:"Art Basel (27–29 Mar)",                                         org:"",               type:"Other" },
  { date:"2026-03-28", month:"March 2026",     region:"Hong Kong",  name:"Art Basel / One Earth Summit",                                  org:"",               type:"Conference" },
  { date:"2026-04-11", month:"April 2026",     region:"Singapore",  name:"Journey of Generosity",                                         org:"Generosity Path",type:"Workshop" },
  { date:"2026-04-14", month:"April 2026",     region:"Hong Kong",  name:"HSBC Global Investment Summit – Wealth (14–16 Apr)",            org:"HSBC",           type:"Forum" },
  { date:"2026-04-17", month:"April 2026",     region:"Hong Kong",  name:"Rugby 7s (17–19 Apr)",                                          org:"",               type:"Other" },
  { date:"2026-04-24", month:"April 2026",     region:"Singapore",  name:"Journey of Generosity",                                         org:"Generosity Path",type:"Workshop" },
  { date:"2026-05-03", month:"May 2026",       region:"USA",        name:"Milken Global Conference (3–6 May)",                            org:"",               type:"Conference" },
  { date:"2026-05-04", month:"May 2026",       region:"London",     name:"Alpha Leadership Conference (4–5 May)",                         org:"Alpha",          type:"Conference" },
  { date:"2026-06-03", month:"June 2026",      region:"Hong Kong",  name:"Sustainable Finance Initiative (Family Office Focus)",          org:"",               type:"Forum" },
  { date:"2026-07-04", month:"July 2026",      region:"Singapore",  name:"Alpha Marketplace",                                             org:"Alpha",          type:"Networking" },
  { date:"2026-07-11", month:"July 2026",      region:"Singapore",  name:"Journey of Generosity",                                         org:"Generosity Path",type:"Workshop" },
  { date:"2026-07-19", month:"July 2026",      region:"USA",        name:"Christian Economic Forum (19–22 Jul)",                          org:"",               type:"Conference" },
  { date:"2026-07-30", month:"July 2026",      region:"Singapore",  name:"APAC Redemptive Business Summit (30–31 Jul)",                   org:"",               type:"Conference" },
  { date:"2026-08-20", month:"August 2026",    region:"Malaysia",   name:"Justice Conference Asia",                                       org:"",               type:"Conference" },
  { date:"2026-08-25", month:"August 2026",    region:"Hong Kong",  name:"AVPN Philanthropy Forum (25–27 Aug)",                           org:"AVPN",           type:"Forum" },
  { date:"2026-08-28", month:"August 2026",    region:"Singapore",  name:"Journey of Generosity",                                         org:"Generosity Path",type:"Workshop" },
  { date:"2026-09-04", month:"September 2026", region:"Singapore",  name:"IDMC Conference (4–5 Sep)",                                     org:"",               type:"Conference" },
  { date:"2026-09-18", month:"September 2026", region:"Singapore",  name:"Faith-Driven Entrepreneurs Conference",                         org:"FDE",            type:"Conference" },
  { date:"2026-09-24", month:"September 2026", region:"Singapore",  name:"N5 Conference Day 1 — For Financial Planners",                  org:"N5",             type:"Conference" },
  { date:"2026-09-24", month:"September 2026", region:"Malaysia",   name:"The Business Network Conference (Jakarta)",                     org:"",               type:"Conference" },
  { date:"2026-09-25", month:"September 2026", region:"Singapore",  name:"Stewardship Conference — N5 (Open to All) / SG500 (25–28 Sep)",org:"N5",             type:"Conference" },
  { date:"2026-09-28", month:"September 2026", region:"Singapore",  name:"SuperReturn Asia (28 Sep–1 Oct)",                               org:"",               type:"Conference" },
  { date:"2026-09-29", month:"September 2026", region:"Singapore",  name:"Tech Week Singapore (29–30 Sep)",                               org:"",               type:"Conference" },
  { date:"2026-10-01", month:"October 2026",   region:"Singapore",  name:"Antioch Summit (1–3 Oct)",                                      org:"",               type:"Gathering" },
  { date:"2026-10-07", month:"October 2026",   region:"Singapore",  name:"Token (7–8 Oct) / Milken Asia Summit (7–9 Oct)",                org:"",               type:"Conference" },
  { date:"2026-10-29", month:"October 2026",   region:"Singapore",  name:"SWITCH — Singapore Week of Innovation & Technology (29–31 Oct)",org:"",               type:"Conference" },
  { date:"2026-11-05", month:"November 2026",  region:"Singapore",  name:"Asia Calling (5–7 Nov)",                                        org:"",               type:"Gathering" },
  { date:"2026-11-18", month:"November 2026",  region:"Singapore",  name:"Praxis Accelerator Finale & Regional Gathering (18–20 Nov)",    org:"Praxis",         type:"Gathering" },
  { date:"2026-11-18", month:"November 2026",  region:"Singapore",  name:"Singapore Fintech Festival (18–20 Nov)",                        org:"",               type:"Conference" },
];

let EVENTS = [];
let activeMonth  = null;
let activeRegion = null;

function regionClass(r) {
  if (/singapore/i.test(r)) return "region-sg";
  if (/hong kong/i.test(r)) return "region-hk";
  if (/malaysia/i.test(r)) return "region-my";
  if (/usa|united states/i.test(r)) return "region-usa";
  if (/london|uk/i.test(r)) return "region-uk";
  if (/,/.test(r)) return "region-multi";
  return "region-glob";
}

function isOverseas(r) { return !/singapore|malaysia/i.test(r); }

function render(events) {
  const root  = document.getElementById("calendar-root");
  const empty = document.getElementById("empty-state");
  root.innerHTML = "";

  const months = {};
  const order  = ["January 2026","February 2026","March 2026","April 2026","May 2026","June 2026",
                   "July 2026","August 2026","September 2026","October 2026","November 2026","December 2026"];

  events.forEach(ev => { (months[ev.month] = months[ev.month]||[]).push(ev); });

  let total = 0;
  order.forEach(month => {
    if (!months[month]) return;
    total += months[month].length;
    const mg = document.createElement("div");
    mg.className = "month-group";
    mg.innerHTML = `
      <div class="month-heading">
        <h3>${month}</h3>
        <div class="month-heading-line"></div>
        <span class="month-count">${months[month].length} event${months[month].length!==1?'s':''}</span>
      </div>
      <div class="events-list" id="list-${month.replace(/\s/g,'_')}"></div>`;
    root.appendChild(mg);

    const list = mg.querySelector(".events-list");
    months[month].forEach(ev => {
      const d   = new Date(ev.date + "T00:00:00");
      const day = d.getDate();
      const dow = d.toLocaleDateString('en-SG', { weekday:'short' });
      const card = document.createElement("div");
      card.className = "event-card";
      card.innerHTML = `
        <div class="ev-date">
          <span class="ev-date-day">${String(day).padStart(2,'0')}</span>
          <span class="ev-date-dow">${dow}</span>
        </div>
        <div class="ev-region"><span class="region-pill ${regionClass(ev.region)}">${ev.region}</span></div>
        <div class="ev-type"><span class="type-badge">${ev.type||'Event'}</span></div>
        <div class="ev-body"><div class="ev-name">${ev.name}</div></div>
        <div class="ev-org">${ev.org?`<span class="org-tag">${ev.org}</span>`:''}</div>`;
      list.appendChild(card);
    });
  });

  empty.classList.toggle("visible", total === 0);
}

function applyFilters() {
  let f = EVENTS;
  if (activeMonth)  f = f.filter(e => e.month === activeMonth);
  if (activeRegion) {
    if (activeRegion === "Overseas") f = f.filter(e => isOverseas(e.region));
    else f = f.filter(e => e.region.toLowerCase().includes(activeRegion.toLowerCase()));
  }
  render(f);
}

function filterAll() {
  activeMonth = activeRegion = null;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  document.querySelector('.filter-btn').classList.add('active');
  render(EVENTS);
}

function filterMonth(btn, val) {
  document.querySelectorAll('.filter-btn').forEach(b => {
    if (b.textContent.trim().length <= 3) b.classList.remove('active');
  });
  document.querySelector('.filter-btn').classList.remove('active');
  if (activeMonth === val) { activeMonth = null; }
  else { activeMonth = val; btn.classList.add('active'); }
  if (!activeMonth && !activeRegion) document.querySelector('.filter-btn').classList.add('active');
  applyFilters();
}

function filterRegion(btn, val) {
  const regionBtns = ['🇸🇬 Singapore','🇭🇰 Hong Kong','🇲🇾 Malaysia','✈ Overseas'];
  document.querySelectorAll('.filter-btn').forEach(b => {
    if (regionBtns.some(r => b.textContent.trim() === r)) b.classList.remove('active');
  });
  document.querySelector('.filter-btn').classList.remove('active');
  if (activeRegion === val) { activeRegion = null; }
  else { activeRegion = val; btn.classList.add('active'); }
  if (!activeMonth && !activeRegion) document.querySelector('.filter-btn').classList.add('active');
  applyFilters();
}

async function loadCalendar() {
  if (CALENDAR_CSV_URL) {
    try {
      const res = await fetch(CALENDAR_CSV_URL);
      const text = await res.text();
      EVENTS = parseCSV(text).filter(r => r.date && r.name);
    } catch(e) {
      console.warn('Calendar sheet failed, using fallback.', e);
      EVENTS = FALLBACK_EVENTS;
    }
  } else {
    EVENTS = FALLBACK_EVENTS;
  }
  render(EVENTS);
}

/* ═══════════════════════════════════════════════════════════
   SECTION 3: RESOURCES
═══════════════════════════════════════════════════════════ */
const FALLBACK_RESOURCES = [
  { category:"ministry", name:"Faith Driven Investor",           url:"https://faithdriveninvestor.org/",              contact:"Singapore: faithdriveninvesting@gmail.com · Global: team@faithdriveninvestor.org", active:"TRUE" },
  { category:"ministry", name:"Compass — Finances God's Way",   url:"https://compass1.asia/",                        contact:"", active:"TRUE" },
  { category:"ministry", name:"N5",                              url:"https://n5stewards.asia/",                       contact:"", active:"TRUE" },
  { category:"ministry", name:"Financial Stewardship Ministries",url:"https://www.financialstewardship.sg/",          contact:"", active:"TRUE" },
  { category:"ministry", name:"Generosity Path Singapore",       url:"https://generositypath.org/",                   contact:"", active:"TRUE" },
  { category:"books",    name:"Good Investor",                   url:"https://www.goodinvestor.com/",                  contact:"", active:"TRUE" },
  { category:"books",    name:"Neither Poverty nor Riches — A Biblical Theology of Possessions", url:"https://www.amazon.sg/Neither-Poverty-nor-Riches-Possessions/dp/0830826076", contact:"", active:"TRUE" },
  { category:"books",    name:"Shrewd Samaritan: Faith, Economics, and the Road to Loving our Global Neighbour", url:"https://www.amazon.sg/Shrewd-Samaritan-Economics-Loving-Neighbor/dp/0785221522", contact:"", active:"TRUE" },
  { category:"books",    name:"Stewards Not Owners: The Joy of Aligning Your Money with Your Faith", url:"https://www.amazon.com/Stewards-Not-Owners-Aligning-Money/dp/1637633750", contact:"", active:"TRUE" },
  { category:"books",    name:"Redemptive Investing — Praxis Playbook", url:"https://www.praxis.co/playbooks/playbook/redemptive-investing", contact:"", active:"TRUE" },
  { category:"books",    name:"Redemptive Philanthropy — Praxis Playbook", url:"https://www.praxis.co/playbooks/playbook/redemptive-philanthropy", contact:"", active:"TRUE" },
  { category:"books",    name:"Money, Possessions, and Eternity", url:"https://www.amazon.sg/dp/0842353607", contact:"", active:"TRUE" },
  { category:"books",    name:"When Helping Hurts",              url:"https://www.amazon.sg/When-Helping-Hurts-Steve-Corbett/dp/0802409989", contact:"", active:"TRUE" },
  { category:"books",    name:"As You Sow — The Adventure of Giving by Faith", url:"https://www.amazon.com/As-You-Sow-Adventure-Giving/dp/089840262X", contact:"", active:"TRUE" },
  { category:"podcast",  name:"Faith Driven Investor Podcast",   url:"https://faithdriveninvestor.org/podcast/",      contact:"", active:"TRUE" },
  { category:"podcast",  name:"7 Deadly Sins: Greed",            url:"https://open.spotify.com/episode/167fRopwavHeRN3owUJPN8", contact:"", active:"TRUE" },
  { category:"podcast",  name:"God & Money — Bible Project",     url:"https://open.spotify.com/episode/34vrmLBuo09U8JTkwc8bTl", contact:"", active:"TRUE" },
  { category:"podcast",  name:"God as the Generous Host",        url:"https://open.spotify.com/episode/0sNadH078ZQukDJBl6pwLM", contact:"", active:"TRUE" },
];

const CATEGORY_META = {
  ministry: { label: "Ministries & Organisations", cssClass: "ministry" },
  books:    { label: "Books",                       cssClass: "books"    },
  podcast:  { label: "Podcasts & Audio",            cssClass: "podcast"  },
};
const CATEGORY_ORDER = ["ministry","books","podcast"];

function renderResources(data) {
  const container = document.getElementById('res-body-dynamic');
  container.innerHTML = '';
  const active = data.filter(r => r.active !== 'FALSE' && r.category && r.name);

  CATEGORY_ORDER.forEach(cat => {
    const items = active.filter(r => r.category.toLowerCase() === cat);
    if (!items.length) return;
    const meta = CATEGORY_META[cat] || { label: cat, cssClass: cat };
    const shortUrl = url => {
      try { return new URL(url).hostname.replace('www.',''); }
      catch(e) { return url; }
    };
    const section = document.createElement('div');
    section.className = 'res-category';
    section.innerHTML = `
      <div class="res-category-heading">
        <h3>${meta.label}</h3>
        <div class="res-category-line"></div>
        <span class="res-category-count">${items.length} resource${items.length!==1?'s':''}</span>
      </div>
      <div class="res-cards" id="res-cards-${cat}"></div>`;
    container.appendChild(section);
    const grid = section.querySelector(`#res-cards-${cat}`);
    items.forEach(item => {
      const card = document.createElement('a');
      card.className = `res-card ${meta.cssClass}`;
      if (item.url) { card.href = item.url; card.target = '_blank'; card.rel = 'noopener'; }
      card.innerHTML = `<div class="res-card-name">${item.name}</div>
        ${item.url ? `<span class="res-card-link">${shortUrl(item.url)}</span>` : ''}
        ${item.contact ? `<div class="res-card-contact">${item.contact.replace(/·/g,'<br>')}</div>` : ''}`;
      grid.appendChild(card);
    });
  });
}

async function loadResources() {
  let data;
  if (RESOURCES_CSV_URL) {
    try {
      const res = await fetch(RESOURCES_CSV_URL);
      const text = await res.text();
      data = parseCSV(text);
    } catch(e) {
      console.warn('Resources sheet failed, using fallback.', e);
      data = FALLBACK_RESOURCES;
    }
  } else {
    data = FALLBACK_RESOURCES;
  }
  renderResources(data);
}

/* ═══════════════════════════════════════════════════════════
   SECTION 4: MINISTRIES DIRECTORY
═══════════════════════════════════════════════════════════ */
const FALLBACK_MINISTRIES = [
  { name:"Compass", type:"programme", description:"Biblical financial discipleship through small group Bible studies, teaching people of all ages how to handle money according to Scripture.", tags:"All believers, Small groups, Financial literacy", duration:"9 weeks", cost:"S$39.99", started:"2011", url:"https://compass1.asia", audience:"all-believers", active:"TRUE" },
  { name:"Crown Financial Ministries", type:"programme", description:"Equips Christians to manage finances according to biblical principles, covering budgeting, debt, giving, and long-term stewardship.", tags:"All believers, Budget coaching, Discipleship", duration:"Varies", cost:"Varies", started:"", url:"https://www.crown.org", audience:"all-believers", active:"TRUE" },
  { name:"Faith Driven Investor", type:"programme", description:"Helps accredited investors and finance professionals integrate faith with investing through peer-led Foundation Groups and an annual conference.", tags:"Investors, Finance industry, Peer groups", duration:"6 weeks", cost:"Free", started:"2022", url:"https://faithdriveninvestor.org", audience:"finance-professionals", active:"TRUE" },
  { name:"Financial Stewardship SG", type:"programme", description:"Grassroots ministry by Yu Khing offering workshops, mentoring, and a study guide to help Christians live out biblical financial freedom.", tags:"All believers, Mentoring, Workshops", duration:"1 day workshop", cost:"Free", started:"2024", url:"https://www.financialstewardship.sg", audience:"all-believers", active:"TRUE" },
  { name:"Generosity Path", type:"programme", description:"Hosts Journey of Generosity (JOG) retreats — a safe, no-fundraising space to explore biblical generosity through story and conversation.", tags:"All believers, Generosity, Retreats", duration:"1 day", cost:"Free", started:"2025", url:"https://generositypath.org/regions/asia/singapore/", audience:"all-believers", active:"TRUE" },
  { name:"N5 Stewardship Movement", type:"programme", description:"Inspired by Nehemiah 5, equips Christian financial planners and the Body of Christ in biblical stewardship through coaching, training, and annual conferences.", tags:"Financial planners, Marketplace, Equipping", duration:"1 day", cost:"On enquiry", started:"2021", url:"https://n5stewards.asia", audience:"finance-professionals, all-believers", active:"TRUE" },
  { name:"N5 — Rediscover Biblical Stewardship", type:"conference", description:"Annual Q3 conference gathering 400+ believers to align finances with biblical principles for lasting legacy and kingdom impact.", tags:"All believers, Annual conference, Legacy", duration:"1 day", cost:"Varies", started:"2020", url:"https://n5stewards.asia", audience:"all-believers", active:"TRUE" },
  { name:"Faith Driven Investor Conference", type:"conference", description:"Annual Q1 gathering for accredited investors, wealth advisors, and VC/PE professionals to integrate faith and investing.", tags:"Accredited investors, Annual conference, Impact investing", duration:"5 hours", cost:"Varies", started:"2022", url:"https://faithdriveninvestor.org", audience:"finance-professionals", active:"TRUE" },
  { name:"SG500", type:"conference", description:"A 3-day immersive forum for wealth stewards to catalyse Gospel movements in the 500 largest cities of Asia, convened annually in Singapore.", tags:"Wealth stewards, Mission, Asia cities", duration:"3 days", cost:"On invitation", started:"2024", url:"https://www.instagram.com/sg500asiacities", audience:"finance-professionals", active:"TRUE" },
  { name:"Asia Calling", type:"conference", description:"An annual Q4 gathering building community and capacity among high-capacity givers across Asia. Contact to learn more.", tags:"High-capacity givers, Generosity, Regional", duration:"3 days", cost:"On invitation", started:"2025", url:"", audience:"finance-professionals", active:"TRUE" },
];

let allMinistries = [];
let minFilter = 'all';
let minSearch = '';

function buildMinCard(org) {
  const tags = typeof org.tags === 'string' ? org.tags.split(',').map(t=>t.trim()).filter(Boolean) : (org.tags||[]);
  const audience = typeof org.audience === 'string' ? org.audience.split(',').map(a=>a.trim()).filter(Boolean) : (org.audience||[]);
  const typeClass = org.type === 'programme' ? 'min-programme' : 'min-conference';
  const typeLabel = org.type === 'programme' ? 'Programme' : 'Conference';
  const isInsta = org.url && org.url.includes('instagram');
  const linkLabel = isInsta ? 'Instagram' : 'Visit website';
  const linkHtml = org.url
    ? `<a class="min-card-link" href="${org.url}" target="_blank" rel="noopener">${linkLabel}</a>`
    : `<span class="min-no-link">Contact to learn more</span>`;
  const tagsHtml = tags.map(t=>`<span class="min-tag">${t}</span>`).join('');
  const card = document.createElement('article');
  card.className = 'min-card';
  card.dataset.type = org.type;
  card.dataset.audience = JSON.stringify(audience);
  card.dataset.search = `${org.name} ${org.description} ${tags.join(' ')}`.toLowerCase();
  card.innerHTML = `
    <div class="min-card-header">
      <h3 class="min-card-name">${org.name}</h3>
      <span class="min-type-pill ${typeClass}">${typeLabel}</span>
    </div>
    <p class="min-card-desc">${org.description}</p>
    <div class="min-tags">${tagsHtml}</div>
    <div class="min-meta">
      <div class="min-meta-cell">Duration<strong>${org.duration||'—'}</strong></div>
      <div class="min-meta-cell">Cost<strong>${org.cost||'—'}</strong></div>
      <div class="min-meta-cell">Since<strong>${org.started||'—'}</strong></div>
    </div>
    <div class="min-card-footer">${linkHtml}</div>`;
  return card;
}

function renderMinistries() {
  const grid = document.getElementById('min-grid');
  const metaEl = document.getElementById('min-meta');
  grid.innerHTML = '';

  const filtered = allMinistries.filter(org => {
    const audience = typeof org.audience === 'string' ? org.audience.split(',').map(a=>a.trim()) : (org.audience||[]);
    const mf = minFilter === 'all'
      || (minFilter === 'programme' && org.type === 'programme')
      || (minFilter === 'conference' && org.type === 'conference')
      || audience.includes(minFilter);
    const ms = !minSearch || `${org.name} ${org.description} ${org.tags||''}`.toLowerCase().includes(minSearch);
    return mf && ms;
  });

  if (filtered.length === 0) {
    const empty = document.createElement('div');
    empty.className = 'min-empty';
    empty.innerHTML = '<p>No ministries match. Try different filters or keywords.</p>';
    grid.appendChild(empty);
  } else {
    filtered.forEach(org => grid.appendChild(buildMinCard(org)));
  }
  metaEl.textContent = `Showing ${filtered.length} of ${allMinistries.length} ministries`;
}

async function loadMinistries() {
  let raw;
  if (MINISTRIES_CSV_URL) {
    try {
      const res = await fetch(MINISTRIES_CSV_URL);
      const text = await res.text();
      raw = parseCSV(text).filter(r => r.active !== 'FALSE' && r.name);
    } catch(e) {
      console.warn('Ministries sheet failed, using fallback.', e);
      raw = FALLBACK_MINISTRIES;
    }
  } else {
    raw = FALLBACK_MINISTRIES;
  }
  allMinistries = raw;
  renderMinistries();
}

document.querySelectorAll('.min-filter-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    document.querySelectorAll('.min-filter-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    minFilter = btn.dataset.minfilter;
    renderMinistries();
  });
});

document.getElementById('min-search').addEventListener('input', e => {
  minSearch = e.target.value.toLowerCase().trim();
  renderMinistries();
});

/* ═══════════════════════════════════════════════════════════
   INIT — load all three sheets on page load
═══════════════════════════════════════════════════════════ */
loadCalendar();
loadResources();
loadMinistries();
</script>
</body>
</html>
